# api/simulate.js is the pre-v2.3 AI endpoint; nothing calls it since /api/game
# + /api/narrative took over. Excluded from deployment ONLY (kept in the repo)
# to stay under Vercel's 12-serverless-function limit when api/v3meta.js ships.
api/simulate.js
