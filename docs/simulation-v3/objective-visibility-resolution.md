<!-- RENDERED FROM ARTIFACT — do not edit by hand. -->
<!-- source: objective-visibility-resolution.json  outputHash: f764f904a1a197f6520270da3c375aa604f6d8a13a473de0c95c9cc723d2168c -->
<!-- generated by: npm run calibration:c6:report  from: npm run calibration:c6:visibility -->
# Objective-visibility resolution

Parameters read from `data/calibration/c5/candidate-history.json -> adjudicability.blindParameters`.

- unseen by the primary objective: **3**
- direction confirmed: **3**
- mechanically consistent: **3**
- frozen at default: **3**
- parameter search performed: **no**

Values were measured at the registry minimum and maximum to establish DIRECTION. No value was scored against an objective, ranked, or selected. Every parameter remains at its registry default.

## Classes

- `ADJUDICATED_BY_PRIMARY_OBJECTIVE`
- `ADJUDICATED_BY_CONDITIONAL_MECHANICAL_TARGET`
- `DEFAULT_FROZEN_UNADJUDICATED`
- `GUARDRAIL_ONLY`

## Summary

| Parameter | Module | Mechanic | Activated | Objective cells differing | Direction confirmed | Default | Candidate | Final class |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `opportunity.mismatch.severe` | opportunityAllocation | `mismatch:SEVERE` | 20192 | 0/288 | true | 2.6 | 2.6 | `DEFAULT_FROZEN_UNADJUDICATED` |
| `zone.highPostVulnerability` | zoneResolution | `zoneGap:HIGH_POST` | 1268 | 0/288 | true | 1 | 1 | `DEFAULT_FROZEN_UNADJUDICATED` |
| `zone.cornerVulnerability` | zoneResolution | `zoneGap:CORNER` | 9031 | 0/288 | true | 1 | 1 | `DEFAULT_FROZEN_UNADJUDICATED` |

### `opportunity.mismatch.severe`

- claim: a larger bias concentrates the mismatch possessions on its beneficiary
- mechanic: `mismatch:SEVERE` — possessions carrying a SEVERE mismatch
- activated possessions: 20192
- primary objective: **blind** (0/288 cells differ, measured at the FULL registry range, wider than the search's own movement cap)
- primaryConcentration: -0.01151 (t -6.1438) -> 0.0086 (t 5.0614)

- guardrail breaches: none
- direction confirmed: **true** · mechanically consistent: **true**
- prior readiness: `STRUCTURAL_CALIBRATION_ONLY` -> final: `DEFAULT_FROZEN_UNADJUDICATED`

Phase 6C2C5 filed this as STRUCTURAL_CALIBRATION_ONLY, which implies a bounded search could tune it. That search then proved the only available numeric objective cannot see the parameter at all. DEFAULT_FROZEN_UNADJUDICATED states the true position: identifiable through its own mechanic, unrankable by any available target, frozen at its default.

**What this establishes.** The mechanic responds to the parameter, in the declared direction, monotonically across the full registry range, without breaching a guardrail.

**What it does not.** A magnitude. Sign and monotonicity do not select a value, so this target cannot prefer the default over any other value in range. The default is retained because nothing available can rank values, not because it was shown to be best.

### `zone.highPostVulnerability`

- claim: higher vulnerability draws more high-post attacks and better looks
- mechanic: `zoneGap:HIGH_POST` — high-post attacks against a live zone
- activated possessions: 1268
- primary objective: **blind** (0/288 cells differ, measured at the FULL registry range, wider than the search's own movement cap)
- expectedMake: -0.02073 (t -3.3936) -> 0.02166 (t 7.3304)
- count: -3.42969 (t -26.5573) -> 4.02734 (t 26.334)
- guardrail breaches: none
- direction confirmed: **true** · mechanically consistent: **true**
- prior readiness: `STRONGLY_REGULARIZED_CALIBRATION` -> final: `DEFAULT_FROZEN_UNADJUDICATED`

Phase 6C2C5 filed this as STRONGLY_REGULARIZED_CALIBRATION, which implies a bounded search could tune it. That search then proved the only available numeric objective cannot see the parameter at all. DEFAULT_FROZEN_UNADJUDICATED states the true position: identifiable through its own mechanic, unrankable by any available target, frozen at its default.

**What this establishes.** The mechanic responds to the parameter, in the declared direction, monotonically across the full registry range, without breaching a guardrail.

**What it does not.** A magnitude. Sign and monotonicity do not select a value, so this target cannot prefer the default over any other value in range. The default is retained because nothing available can rank values, not because it was shown to be best.

### `zone.cornerVulnerability`

- claim: higher vulnerability draws more corner attacks and better looks
- mechanic: `zoneGap:CORNER` — corner attacks against a live zone
- activated possessions: 9031
- primary objective: **blind** (0/288 cells differ, measured at the FULL registry range, wider than the search's own movement cap)
- expectedMake: -0.00177 (t -1.2004) -> 0.02977 (t 26.3643)
- count: -8.14844 (t -23.5665) -> 8.82031 (t 22.0435)
- guardrail breaches: none
- direction confirmed: **true** · mechanically consistent: **true**
- prior readiness: `STRONGLY_REGULARIZED_CALIBRATION` -> final: `DEFAULT_FROZEN_UNADJUDICATED`

Phase 6C2C5 filed this as STRONGLY_REGULARIZED_CALIBRATION, which implies a bounded search could tune it. That search then proved the only available numeric objective cannot see the parameter at all. DEFAULT_FROZEN_UNADJUDICATED states the true position: identifiable through its own mechanic, unrankable by any available target, frozen at its default.

**What this establishes.** The mechanic responds to the parameter, in the declared direction, monotonically across the full registry range, without breaching a guardrail.

**What it does not.** A magnitude. Sign and monotonicity do not select a value, so this target cannot prefer the default over any other value in range. The default is retained because nothing available can rank values, not because it was shown to be best.

## Readiness v4

| Readiness class | Count |
|---|---|
| `DEFAULT_FROZEN_TRUE_NO_EFFECT` | 29 |
| `STRUCTURAL_CALIBRATION_ONLY` | 4 |
| `STRONGLY_REGULARIZED_CALIBRATION` | 4 |
| `DEFAULT_FROZEN_PENDING_EXTERNAL_DATA` | 9 |
| `DEFAULT_FROZEN_CONFOUNDED` | 2 |
| `DEFAULT_FROZEN_GUARDRAIL` | 2 |
| `DEFAULT_FROZEN_UNADJUDICATED` | 3 |
| **TOTAL** | **53** |

Reconciles: **true** against 53 active parameters.

All values at registry default: **true**.
