<!-- RENDERED FROM ARTIFACT — do not edit by hand. -->
<!-- source: target-schema-validation.json  outputHash: dc9dd145096df6c3e607185b3492119e8c50c6514231d60041d456c66001ec9d -->
<!-- generated by: npm run c1:report  from: npm run c1:instrumentation -->
# Validation instrumentation repairs — Phase 6C4A

Three instrumentation defects from 6C3R, repaired at the shared-module level
and proven against the real stores.

## 1. Typed target access (`readTargetValue`)

6C3R census read availability by object truthiness; an entry {value:null} is truthy, so it reported every field available.

| census | count |
| --- | ---: |
| target entries | 930 |
| usable under the typed accessor | **93** |
| "available" under object truthiness | 930 |
| truthiness over-report | **837** |
| schema violations | 0 |

Value-bearing statuses: `RECORDED_STATISTIC`, `DERIVED_FROM_AUTHORIZED_TOTALS`. Everything else is
typed as unusable — object truthiness is not evidence.

## 2. Exact profile resolution

| fact | value |
| --- | ---: |
| combined profiles (v3 + v4) | 330 |
| unresolved | 0 |
| duplicate ids | 0 |
| last-name collisions (distinct persons sharing a surname) | 18 |
| alias titles (exact, recorded) | 6 |
| bare-surname resolutions | **0** |

Profiles are resolved by exact calibrationPlayerId; profile articles by exact (disambiguated) title. Last-name-only matching is prohibited: the combined store proves it ambiguous.

## 3. Runner preflight on the exact profile map

The V4 dry run preflighted a simplified V4-only map; the real runner crashed on the first era-reference build AFTER consuming its unlock.

A sealed-set runner preflights profile resolution through buildRunnerProfileMap() — the exact map the run dereferences — over every fixture player AND every era-reference five member, before any unlock is consumed.

| preflight | result |
| --- | ---: |
| required profile ids (fixtures + era-reference fives) | 195 |
| resolved through `buildRunnerProfileMap()` | 195 |
| the simplified V4-only map would have missed | **40** |
