{
  "artifact": "account-schema-contract",
  "phase": "9B.1 — real accounts, cloud career, My EraClash",
  "mode": "migrations",
  "baseUrl": null,
  "provider": "supabase (auth + postgres + row level security)",
  "cloudAccountsConfigured": false,
  "cloudAccountsReady": false,
  "checks": 11,
  "passed": 11,
  "failed": 0,
  "results": [
    {
      "name": "one versioned migration exists and records its own version",
      "pass": true,
      "detail": ""
    },
    {
      "name": "every user-owned table exists with a user_id bound to auth.users",
      "pass": true,
      "detail": ""
    },
    {
      "name": "a profile is unique per user and its name is length- and content-constrained",
      "pass": true,
      "detail": ""
    },
    {
      "name": "one clash per user per result, and one owner per result",
      "pass": true,
      "detail": ""
    },
    {
      "name": "the result id shape is enforced in the database, not only in the client",
      "pass": true,
      "detail": ""
    },
    {
      "name": "career statistics are derived views, not mutable counters that can drift",
      "pass": true,
      "detail": ""
    },
    {
      "name": "no email column, no seed, no key, no password anywhere in the schema",
      "pass": true,
      "detail": ""
    },
    {
      "name": "the device session is stored only as a 64-hex hash",
      "pass": true,
      "detail": ""
    },
    {
      "name": "a snapshot is stored so a saved report survives the result cache expiring",
      "pass": true,
      "detail": ""
    },
    {
      "name": "exactly one profile is created per sign-up, idempotently",
      "pass": true,
      "detail": ""
    },
    {
      "name": "deleting an account cascades rather than orphaning private career data",
      "pass": true,
      "detail": ""
    }
  ],
  "schema": {
    "migration": "supabase/migrations/0001_accounts.sql",
    "sha256": "de9c9abdb6c734729a67bfff2754f90e6c01e9e81964a8054070cc5771a24b19",
    "tables": [
      "profiles",
      "saved_clashes",
      "result_claims"
    ],
    "views": [
      "career_summary",
      "career_by_mode",
      "career_streak"
    ]
  },
  "status": "FROZEN",
  "migration": {
    "path": "supabase/migrations/0001_accounts.sql",
    "sha256": "de9c9abdb6c734729a67bfff2754f90e6c01e9e81964a8054070cc5771a24b19",
    "version": "0001_accounts"
  },
  "tables": {
    "profiles": {
      "key": "user_id → auth.users (cascade)",
      "columns": [
        "display_name (1-24, no <>)",
        "avatar_url (https only)",
        "created_at",
        "updated_at"
      ],
      "emailColumn": false,
      "createdBy": "the handle_new_user trigger, once per account"
    },
    "saved_clashes": {
      "key": "unique (user_id, result_id)",
      "authoritativeSource": "the result record in the store",
      "snapshot": "result_snapshot, minus the device session",
      "candidateIdentity": [
        "candidate_id",
        "calibration_version",
        "candidate_core_hash"
      ],
      "challenge": "challenge_fingerprint (sha256, truncated) — never the seed"
    },
    "result_claims": {
      "key": "result_id PRIMARY KEY — one result, one owner",
      "stores": "device_session_hash (sha256, 64 hex) — never the session"
    }
  },
  "careerStatistics": {
    "method": "derived views, not mutable counters",
    "views": [
      "career_summary",
      "career_by_mode",
      "career_streak"
    ],
    "security": "security_invoker = true, so the table's policies isolate each career"
  },
  "fabricated": {
    "rank": false,
    "contenderGrade": false,
    "percentile": false,
    "leaderboardPosition": false
  },
  "deletion": {
    "cascadesFromAuthUsers": true,
    "selfService": false,
    "limitation": "self-service deletion and export are deferred to an account-hardening phase and are stated on the career page; this build is not public-launch ready without them"
  }
}
