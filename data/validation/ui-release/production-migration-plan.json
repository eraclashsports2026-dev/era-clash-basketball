{
  "artifact": "production-migration-plan",
  "status": "PREPARED — NOT EXECUTED. Nothing in this plan has been run against any production system.",
  "release": "ui/light-court-release-candidate",
  "rcCommit": "149eef3 (code) — see ui-release-final-summary.json for the certified head",
  "recordedAt": "2026-09-09",
  "currentProduction": { "origin": "https://www.eraclashbasketball.com (apex 308 -> www)", "deployment": "main@9cd95ff v2.7.2, deployed 2026-08-24", "database": "NONE — production has zero Supabase migrations and no cloud accounts; the only Supabase project (lfybiphmqkiecfrqsfzt) is the PREVIEW database and must never become production's" },
  "principle": "Production gets its OWN Supabase project. Preview user data, QA fixtures and test accounts are never copied. The preview project is never pointed at by production env vars.",
  "steps": [
    { "n": 1, "actor": "owner", "action": "Create a new Supabase project for production (region of choice). Record the project ref. Do not reuse lfybiphmqkiecfrqsfzt.", "verify": "Project ref differs from the preview ref." },
    { "n": 2, "actor": "owner/operator", "action": "Apply supabase/migrations 0001..0007 IN ORDER, exactly as committed at the RC commit, via the Supabase SQL editor or CLI (supabase db push against the new ref).", "files": ["0001_accounts.sql", "0002_accounts_hardening.sql", "0003_career_v2.sql", "0004_challenges.sql", "0005_progression_v1.sql", "0006_competitive_rating_v1.sql", "0007_public_competitive_profiles_v1.sql"], "verify": "select count(*) from supabase_migrations.schema_migrations (or the CLI list) = 7; every function body byte-identical to the committed file (the 9E/9F live-certification procedure)." },
    { "n": 3, "actor": "owner", "action": "Authentication -> URL Configuration on the NEW project: Site URL https://www.eraclashbasketball.com; redirect URLs https://www.eraclashbasketball.com/auth/callback and https://eraclashbasketball.com/auth/callback. Email provider per docs/accounts/eraclash-account-provider-setup.md (implicit flow, not PKCE).", "verify": "A magic-link sign-in from the production domain lands on /auth/callback." },
    { "n": 4, "actor": "owner", "action": "Vercel -> Project -> Environment Variables, PRODUCTION scope only, set from the NEW project (names only; values never recorded here): SUPABASE_URL, SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, VITE_SUPABASE_URL, VITE_SUPABASE_ANON_KEY, CLOUD_ACCOUNTS_ENABLED=true. Keep KV_REST_API_URL/KV_REST_API_TOKEN (or UPSTASH_*), ANTHROPIC_API_KEY, RL_* as today.", "verify": "Key KIND check (anon vs service role by shape) per the 9B.1 hazard record; the service role key exists only in the server scope, never in a VITE_ variable." },
    { "n": 5, "actor": "owner decision", "action": "Feature flags for production (defaults differ from preview): CHAOS_CLASH_ENABLED=true (default is preview-only), PREVIEW_SIM_ENGINE_ENABLED — decide whether Candidate 4 (the certified preview engine) or the production engine 3.2.0 fallback serves production; CHALLENGES_ENABLED / PUBLIC_LEADERBOARD_ENABLED / AI_NARRATIVE_ENABLED default true; MAINTENANCE_MODE unset.", "verify": "/api/health on the promoted deployment reports the intended candidate and cloudAccounts.enabled=true." },
    { "n": 6, "actor": "owner", "action": "Promote the RC commit: merge chain phase-9f -> phase-9b3 -> ... -> main per the wave process (PR #48 merged into phase-9b3 already; PR #49 and the RC PR are open drafts). Vercel Git-integration builds main; the SW cache identity changes with the build so clients refresh on next load.", "verify": "Production /api/health build matches the RC package version; Home renders the Light Court; header obsidian." },
    { "n": 7, "actor": "operator", "action": "Run the deployed gates against https://www.eraclashbasketball.com with authorised fixtures, then DELETE the fixtures: chaos:deployed-qa, accounts (manual sign-in), challenge:deployed-qa, progression:deployed-qa, competitive:deployed-qa, profile:deployed-qa.", "verify": "No QA fixture rows remain in the production database (auth.users count = real users only)." }
  ],
  "notExecutedBecause": "Owner authorisation for production changes was explicitly withheld in this brief; this plan awaits 'APPROVE UI — PREPARE FINAL DOMAIN PROMOTION' and then a separate execution approval."
}
