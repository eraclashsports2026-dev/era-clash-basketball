// ── The mode signature ids, by name — no JSX, so node scripts and tests can read it ─
// ModeSignature.jsx draws exactly these seven; the registry's `visualSignature`
// must be one of them (pinned by tests/v9a3p-play-lobby-polish.test.js).
export const SIGNATURE_IDS = Object.freeze(["fracture-dice", "crossing-timelines", "spotlight-calendar", "series-ticks", "season-arc", "bracket", "era-steps"]);
